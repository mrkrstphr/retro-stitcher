export default function ColorDisclaimer({ props }) {
  return (
    <div {...props}>
      * DMC does not publish official names for their colors. Different sources will use different
      names.
    </div>
  );
}
